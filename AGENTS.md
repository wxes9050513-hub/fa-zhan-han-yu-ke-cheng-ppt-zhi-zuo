# 發展漢語課堂投影片

本專案的使用者是製作 KN2／KN4 課程的教師。學生畫面使用越南語操作說明與簡體中文；教師文件保留繁體中文。

- 開始或續做一課：先讀 `製作契約.md`、本課課本／教材包與教師核可紀錄，再讀 `.agents/skills/course-slide-production/SKILL.md`、該課 01／02 規格與目前製作稿。局部修改不用重讀其他課。
- 本課已確認的內容與教學安排不得因套版而刪改；內容異動先列出確切題目與差異，沿用已有教師授權。
- 新課或內容變更的正式入口是「讀教材 → 建立或更新教材包與結構化 `items` → 來源／內容核對 → 生成投影片 JSON → 可選 editor 微調」。`lessonPackage` 保存教材包，`sources` 逐原教材頁保存文字與來源，`items` 保存穩定 ID、題型、目標、教材／教師補充來源、正面、答案、超綱標注與核對狀態。
- `scripts/prepare_lesson_package.py` 只可讀文字層或 `pdftotext` 能讀出的原生 PDF；掃描 PDF 沒有文字時需先取得 OCR，不能臆造內容。`scripts/build_course.cjs` 從已核對教材包生成可編輯 JSON；AI／教師整理不等於教師核可，程式結構檢查也不取代語意與程度判定。
- 教材包欄位、UI 核對步驟與可選 CLI 參數以 `docs/教材包格式.md` 為準；抽取器輸出的 `items` 為空，必須由 AI／教師依課本整理並逐項核對。
- 風格已定為 Original Course、960 × 540。不要每次重新問風格，也不要套用參考專案的 Adam 專用流程、私人路徑、Google Sheets 或 Huashu 強制設定。
- 上游 ai-teaching-material-system 僅供程式參考，不在本次工作中修改、升級或整套覆蓋。朋友本機產生器可能已有修改，先比對實際版本。
- 不將輸出 HTML 或單頁物件當唯一修正來源。內容修改必須回寫 `items`／來源並使受影響核對失效，重新核對後再生成；位置、尺寸與樣式修改寫入 `layoutOverrides`，重建不得丟失。緊急直接改 HTML 時，同步記錄待回寫項目，完成前不能宣稱可重建。
- 圖片確認沿用 `05_…圖片生成與嵌入流程.md` 的四階段要求，記錄已核可項目；同一素材與同一操作已有有效授權時不再重問。不要以這些圖片閘門阻擋不依賴圖片的文字修正。
- 舊產生器路線執行 `python3 scripts/check_slide_draft.py "本課HTML_PPT製作稿.md"`；此檢查只驗證文件結構，不代表內容、排版或互動通過。editor 路線只有在遷移相容欄位 Markdown 時使用它作來源診斷；新建 `.slides.json` 改跑模型檢查與重新開啟驗證。
- 不在生成前刪除現有投影片；新版本先輸出至暫存資料夾，驗證後切換。只在已指定的本專案或暫存目錄內操作，不讀取帳密資料。

## 本倉庫編輯器路線

- editor/ 的「新課程」會先建立教材包並開啟教材資料庫；新課先讀教材、核對 `sources`／`items`，再生成 `.slides.json`，生成後才進行離線微調、放映與匯出。生成前畫布與格式工具鎖定，不能繞過教材資料庫作自由正式備課。
- 編輯器課程以下載的 `.slides.json` 為可攜來源，檔案內的 `lessonPackage`、`sources`、`items`、`slides` 與 `layoutOverrides` 一起保存；`slides` 陣列取顯示順序，物件以固定 ID 識別。輸出 HTML 只供放映，修正回到 JSON。
- 改題目、答案、學生指令或其他課程內容時，更新對應 `items`／來源並清除該項核對狀態；改位置、尺寸、樣式時只更新 `layoutOverrides`，重建須保留這些版面修改。新增題目必須通過來源、答案、指令與核對檢查。
- 符合 `頁碼`、`檔名`、`投影片畫面文字` 欄位的 Markdown 匯入只是遷移／待溯源草稿，原文存教師備註；保留重複頁碼所指的全部內容，不能把匯入成功當成課堂驗收。舊 JSON 與製作稿可下載備份，但未核對前不得正式放映或匯出。
- editor/model.js 共用渲染供編輯器與匯出使用。維持離線開啟、嵌入圖片、安全文字渲染、復原及可攜備份。
- 修改後跑 Python 文件檢查器測試及 Node 模型測試；涉及互動、存檔、匯出時另做真實瀏覽器驗證。未測瀏覽器與平台不得宣稱通過。
