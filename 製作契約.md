# 課堂投影片製作契約

這份文件供教師與製作助理使用，目的是讓同一項修改能保留到下一次重建，並在批次製作前找出容易溢出的頁型。它不取代 KN2／KN4 的課本、程度與教學安排。

## 哪份資料決定什麼

| 決定事項 | 依據 |
|---|---|
| 教師指定內容、刪留、圖片核可 | 本次有效指示及本課教師核可紀錄；不要臆測缺少的核可 |
| 課文原句、題幹、答案與頁碼 | 課本及已核可改寫；內容疑點列出原句與建議，不因版面不足自行改寫 |
| 共用排版、編號、重建與驗證 | 本契約；其他文件的舊共用數值不得覆蓋它 |
| KN2／KN4 教學結構與頁型內容 | 各課程的 01 架構、02 模板與本課核可內容 |
| 本課實際順序、顯示文字與素材 | 新課或內容變更先由課本／教師核可資料建立教材包；`lessonPackage`、逐頁 `sources` 與結構化 `items` 是內容與來源依據，核對後按 editor UI 生成或以可選的 `build_course.cjs` 生成 `.slides.json`。editor 生成後才作可選微調，版面變更寫入 `layoutOverrides`；舊產生器路線仍使用本課 HTML_PPT 製作表。舊稿與舊 JSON 保留為待溯源資料，避免同時維護兩份未核對的正式來源 |
| 實作方式 | 本倉庫 editor/ 可直接離線編輯；舊課亦可沿用本機產生器。上游專案只作參考，不直接載入個人偏好 |

教師有明確的單頁例外時，記錄課次、頁面內容 ID、例外欄位與原因。只套用到指定頁，不變成所有課的共同規則。無法判定的內容衝突需教師決定；既有共同數值衝突依本契約處理。

## 正式入口與可追溯資料

新課或需要改內容的課程固定依以下順序處理：

1. 讀本課課本與教師核可資料。可用 `scripts/prepare_lesson_package.py` 整理文字檔及有文字層的原生 PDF；掃描 PDF 若沒有文字，必須先取得 OCR，不能從空結果臆造課文、題目或答案。
2. 建立或更新 `.slides.json` 內的 `lessonPackage`。`sources` 逐原教材頁保存頁碼、讀出的文字與來源；`items` 是本課內容資料庫，每項至少保留穩定 ID、題型、教學目標、教材／教師補充來源、正面、答案、超綱標注與核對狀態。
3. AI 或教師可協助整理 `items`，但教師須人工判定語意、程度、題目、答案與指令。通過來源、題型、必填欄位、正反面格式、標注、版本與安全區等可檢查條件，以及教師內容核對後，才可按 editor UI 生成投影片 JSON，或使用可選的 `scripts/build_course.cjs` 批次生成。程式檢查不宣稱能自動判定全部教學品質。
4. 生成後才可用 editor 作拖曳、文字、圖片或其他手動微調。若改題目、答案、學生指令或其他內容，回寫對應 `items`／來源並使該項核對失效，重新核對後再生成或正式交付；若只改位置、尺寸或樣式，寫入 `layoutOverrides`，重建時保留。

舊 `.slides.json`、HTML_PPT 製作稿與符合 `頁碼`、`檔名`、`投影片畫面文字` 欄位的 Markdown 匯入，都是待溯源／遷移資料。匯入可保留全部列供整理與下載備份，但不能繞過來源、答案、指令或核對，也不能在未核對前正式放映或匯出；舊稿中的重複編號不以刪題換取檢查通過。

## 排版只使用同一套座標

- 畫布固定為 960 × 540 **CSS px**，先在未縮放的畫布檢查；投影縮放後的螢幕像素不能拿來反推字級。
- 所有主要內容與頁型卡片距左右各 27 px。生詞右卡寬 546 px，左右卡間距 10 px，右卡內距各 10 px，文字寬 526 px；採 `box-sizing: border-box`。這組數值下，左卡寬為 350 px，右卡從 x=387 到 x=933。不要把卡片內距誤當畫布外距。
- 例句維持 22 pt（約 29.33 CSS px）；段落間距 6 px；一般行高 44 px；含換行超綱標記時外層 60 px、內部行盒 42 px。42 px 是行盒，並非漢字實際可見高度；不為追求 42 px 的字形而放大字級。
- 拼音與越文標記仍用 9.33 px，原有 -5 px／-4 px 作為排版偏移參考，不當成可見字形「負距離」的硬性驗收值。檢查基線對齊、標記不壓到鄰行，並保留實際截圖。
- 上下可用區以實際 Banner、來源與頁碼位置界定，模板必須保留各自區域；不要只檢查左右邊界。
- 同一組 AB 對話保持一個語意區塊，A/B 分行。編號獨立成欄，續行對齊句子首字。
- 超出容量先依完整題目拆頁，保留題目順序與答案。一般語法一組拆頁前預設 3–5 題，續頁可少於 3 題；不能為湊題數補題。KN4 功能句完整保留課本題數，不套用此範圍。
- 封面、長詞卡等原有自適應規則可保留；不可用全課縮字、壓行距或刪除學生必要說明來修正溢出。
- 圖片有教學必要且空間足夠才加入；先用實際文字量測可用空間，再進既有圖片核可流程。

## 把內容與顯示順序分開

`slideId` 是建立頁面後固定的內容 ID，例如 `kn2-l2-vocab-jianyi`。插頁不改 ID；拆頁可加 `-a`、`-b`。尚未採用 ID 的舊稿先按標題與來源核對，不直接把頁碼當永久 ID。

`deckIndex` 是本課輸出順序，由核可頁面清單從 1 連續取得。`fileName` 由它產生，例如 `slide-21.html`。總數、目錄、hash 導覽與頁尾都使用同一份清單。

`textbookPages` 保存課本頁碼或範圍，例如 `13-14`。不得從 `deckIndex` 推算。舊 `lessonPage` 僅追溯教師舊稿，不作課本頁碼。圖片以內容 ID／用途識別；舊圖片檔名含頁碼時，不因插頁而改變圖片所指內容。

現有工具 `check_slide_draft.py` 可從原有製作表讀取頁數與檔名，無須先把所有課改成新格式。它的 JSON 是預檢證據，不是產生器 Manifest 的替代品，也不會自動核可內容。

## 先完成一種頁型，再套用整課

1. **內容準備**：讀取課本與教師核可資料，建立或更新教材包、逐頁來源與 `items`。此時不要求不存在的 HTML 截圖；掃描 PDF 無文字時明確標記需 OCR。
2. **文件預檢**：舊產生器路線檢查重複／跳號、檔名、總數與舊 QA 宣稱，並修正會妨礙生成的檔名、Manifest 對應與已確定錯誤；editor 路線的相容欄位 Markdown 檢查只作遷移前來源診斷，重複頁碼的全部列可保留為待核對草稿。錯誤修正後重新執行，不為通過舊檢查自動刪列或改寫內容。
3. **核對與生成**：完成教師內容核對與必要結構檢查後，按 editor UI「核對完成，生成 PPT」，或使用可選的 `build_course.cjs` 從教材包生成 `.slides.json`；不要把自動生成結果直接標為核可。
4. **代表頁渲染**：使用本課最長的實際內容，檢查字型載入、翻牌正反面、答案隱藏及揭示狀態；只作內部技術檢查，不增加例行核可步驟。
5. **批次生成與可選微調**：生成到新的暫存輸出資料夾，保留目前可上課版本；生成後才可用 editor 微調，內容回寫 `items` 並重核對，版面修改寫入 `layoutOverrides`。
6. **驗證與切換**：核對整課導覽與檔名、圖片載入，測試受影響頁型；新版本通過後才切換入口。生成失敗時繼續使用上一版。

圖片需求、生成方式、裁切素材與嵌入仍依各課程 05 圖片流程確認。用本課既有紀錄寫明已核可的項目與範圍；未變更內容不重問。PDF 備份沿用最終版確認規則，不在每輪調整都匯出。

## 一次修正要留下什麼

在本課製作稿末尾記錄一行：日期、頁面內容 ID／標題、問題、改動來源檔、受影響頁、驗證結果。內容／來源變更只重審受影響內容；樣式變更測受影響頁型，不重新設計整課。

若臨時只能改 HTML，明記「尚未回寫來源，重建會遺失」。在回寫及重建驗證之前，不標為完成。

驗證分開記錄：文件結構、內容核對、渲染、互動、PDF。勾選要有版本或檔案雜湊與證據位置；修改後失效的勾選清空。文件預檢 PASS 絕不等於課堂可用。

## 最少驗證範圍

每次採用舊產生器或遷移製作表時都做文件預檢；新課或內容變更先檢查教材包、逐頁來源、`items` 核對狀態與生成條件，再檢查 `.slides.json` 模型並重新開啟。渲染時檢查實際改頁及同模板代表頁；增刪頁另驗前後頁、重新整理定位及縮圖；舊產生器路線另核對整課檔名與 Manifest。修改共用 CSS、字型或 presenter 才擴大到所有受影響頁型。

舊產生器交付前另測翻牌、逐步答案、全螢幕工具列、註記復原與當頁含註記 PDF；只有實際測過才標記通過。核對學生指令是否能直接執行，例如題幹、作答位置、組別及完成條件不能只放教師備註。

## 編輯器適用範圍

編輯器固定使用同一個 960 × 540 畫布；「新課程」先建立教材包並開啟教材資料庫，完成 `sources`／`items` 核對後才生成，生成後才進行離線微調、放映與匯出。手動編輯的自由物件不宣稱已自動套用以上專用詞卡模板。正式課程的 `.slides.json` 應同時保存 `lessonPackage`、`sources`、`items`、`slides` 與 `layoutOverrides`；`slides` 陣列是顯示順序，頁面與物件以固定 ID 識別。尚未生成時畫布與格式工具保持鎖定；舊稿匯入仍只是技術／遷移草稿，不能取代教材包核對。

editor 的最少檢查是：教材包與逐頁來源存在；每個 `item` 有穩定 ID、題型、目標、來源、正面、答案、必要指令／標注與核對狀態；生成後重開課程檔、檢查文字與圖片、比較調整位置、測試正反面與前後頁、離線開啟匯出 HTML。內容變更要確認回寫 `items` 且該項核對失效，版面變更要確認 `layoutOverrides` 在重建後仍保留。Markdown 文件檢查只作遷移前來源診斷；舊系統註記、含註記 PDF、專用拼音排版不在本版編輯器範圍，不能沿用舊系統勾選宣稱已支援。
